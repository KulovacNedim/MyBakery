package com.bakery.repository;

import com.bakery.model.CompanySocialMedia;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CompanySocialMediaRepository extends JpaRepository<CompanySocialMedia, Long> {
}
